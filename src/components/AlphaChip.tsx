import React from "react";
import styled from "styled-components";

const Styled = styled.div`
  padding: 0px 8px;
  background-color: #e65225;
  color: #fff;
  display: inline-block;
  border-radius: 12px;
`;

export default function AlphaChip() {
  return <Styled>Alpha</Styled>;
}
